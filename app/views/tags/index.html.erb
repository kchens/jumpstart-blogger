<h1>All Tags</h1>

<ul id="tags">
		<% @tags.each do |tag| %>
			<li>
				<% link_to article.title, article_path(article) %>
				<a href="/tags/1">First Tag</a>
				<% link_to "Create a New Tag", new_article_path, class: "new_tag" %>
			</li>
		<% end %>
	</ul>